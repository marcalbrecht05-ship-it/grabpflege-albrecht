Content container: paper surface, 1px hairline, 5px radius, whisper-soft shadow.

```jsx
<Card interactive><SectionHeading eyebrow="Leistung" title="Dauergrabpflege" /></Card>
```

Never use a coloured left border. Hover only lifts when `interactive`.
