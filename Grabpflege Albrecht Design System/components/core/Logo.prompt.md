The brand lockup as supplied — never redraw or recolour it.

```jsx
<Logo variant="wordmark" height={40} assetBase="../assets" />
```

On dark or photographic backgrounds put the lockup on a paper-coloured plate rather than inverting it.
