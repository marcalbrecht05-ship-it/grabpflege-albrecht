Primary action control — uppercase Montserrat, 3px radius, moss fill.

```jsx
<Button variant="primary" icon={<Icon name="phone" size={16} />}>Termin anfragen</Button>
```

Variants: primary (moss), secondary (hairline outline), ghost (text), inverse (paper on charcoal). Sizes sm/md/lg. Press state nudges 1px down — never scales or bounces. Max one primary per view.
