Thin Lucide icon wrapper — use for every glyph in this brand; icons inherit `currentColor`.

```jsx
<Icon name="leaf" size={18} />
```

Notes: Lucide 2px-stroke outline set (substituted — no brand icon assets were provided). Never mix filled icon sets or emoji. Common slugs: leaf, sprout, flower-2, droplets, calendar-days, phone, mail, map-pin, clock, check.
