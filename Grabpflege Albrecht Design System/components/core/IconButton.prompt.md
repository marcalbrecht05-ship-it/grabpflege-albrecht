Square icon-only control for dismiss, navigation and toolbar actions.

```jsx
<IconButton name="x" label="Schließen" variant="ghost" />
```

Always pass `label`. 38px default keeps it comfortable for older users — do not go below 36px on touch surfaces.
