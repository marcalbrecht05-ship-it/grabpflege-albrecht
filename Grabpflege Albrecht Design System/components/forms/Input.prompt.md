Single-line text field with label, hint and error slots.

```jsx
<Input label="Name der Grabstelle" hint="Feldnummer genügt" icon={<Icon name="map-pin" size={16} />} />
```

Focus draws a moss ring; errors switch the border and swap hint for error text.
