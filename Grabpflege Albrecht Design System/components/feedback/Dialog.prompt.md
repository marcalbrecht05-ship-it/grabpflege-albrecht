Centred modal on a charcoal scrim — confirmations and short forms.

```jsx
<Dialog title="Anfrage senden?" footer={<><Button variant="secondary">Abbrechen</Button><Button>Senden</Button></>} onClose={close}>Wir antworten innerhalb von zwei Werktagen.</Dialog>
```
